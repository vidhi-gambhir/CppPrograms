void func(Node *root,int level,vector<int> &x)
    // {
    // if(root==NULL)
    //     return;
    // if(x.size()==level)
    //     x.push_back(root->data);
    // func(root->left,level+1,x);
    // func(root->right,level+1,x);
    // }